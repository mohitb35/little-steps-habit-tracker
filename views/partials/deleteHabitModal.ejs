<div id="delete-habit-modal" class="modal hidden">
	<div class="modal-wrapper">
		<button class="modal-close-button" data-target="delete-habit-modal">
			<img src="/media/cross-icon.svg" alt="Close button" />
		</button>
		<div class="modal-contents">
			<h4 class="modal-title"><%= habit.title %></h4>
			<p class="modal-text">Are you sure you want to delete this habit?</p>
			<div class="modal-actions">
				<form
					id="delete-habit-form"
					action="/habits/<%= habit.id %>?_method=DELETE"
					method="POST"
				>
					<button class="small-button action-desired">Yes</button>
				</form>
				<button
					class="small-button modal-close-button"
					data-target="delete-habit-modal"
				>
					No
				</button>
			</div>
		</div>
	</div>
</div>
